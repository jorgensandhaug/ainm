Summary written. Key finding: correctness is 0.5625 (4.5/8), same [P,F,F,P,P,F] pattern across all 24 attempts. The rate fix (omitting explicit rate=800) had zero effect. The strongest untested hypothesis is that `count` should be 2 (overnights for a 3-day trip) rather than 3 (total days) — this has never been varied across any of the 24 runs and directly affects the amounts likely checked by the 3 failing checks.
