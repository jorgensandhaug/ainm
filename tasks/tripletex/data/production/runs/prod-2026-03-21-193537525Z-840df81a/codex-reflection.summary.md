Score-reflection written to the canonical path. Summary: Task 27 scored 50% (1.5/6) — checks 1-2 passed (payment registered, invoice closed) but checks 3-4 failed (no agio booked). Root cause: the invoice is NOK not EUR, so the `:payment` endpoint cannot auto-book FX gain. The trusted standard's NOK fallback caps at 50%. The critical next step is sandbox-testing a manual `POST /ledger/voucher` with Dr 1920 / Cr 8060 (avoiding account 1500) to determine if agio can be manually booked without corrupting invoice state.
