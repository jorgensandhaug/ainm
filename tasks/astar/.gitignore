# local env + caches only
.venv/
.env
__pycache__/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints/
*.pyc
*.pyo
*.egg-info/

# Keep `data/` visible so raw data and selected artifacts can be versioned.
!data/
!data/**
data/catalog.duckdb

# Derived outputs are reproducible; keep them out of git.
data/derived/
