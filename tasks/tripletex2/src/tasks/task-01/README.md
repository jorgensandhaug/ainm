# Create customer

Typed task surface and draft runtime stub for `create-customer`.

The intended deterministic solve path should follow `codex-environment/trusted-standards/create-customer.md` when this task gets a real strategy implementation.
