# Create department

Typed task surface and draft runtime stub for `create-department`.

The intended deterministic solve path should follow `codex-environment/trusted-standards/create-department.md` when this task gets a real strategy implementation.
