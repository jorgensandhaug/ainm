# Create employee

Typed task surface and draft runtime stub for `create-employee`.

The intended deterministic solve path should follow `docs/trusted-standards/create-employee.md` when this task gets a real strategy implementation.
