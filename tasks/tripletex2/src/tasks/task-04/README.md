# Create product

Typed task surface and draft runtime stub for `create-product`.

The intended deterministic solve path should follow `docs/trusted-standards/create-product.md` when this task gets a real strategy implementation.
